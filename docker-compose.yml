version: "3"
services:
    rodo-api:
        container_name: rodo-api
        build:
            context: .
            dockerfile: Dockerfile
        # expose:
        #     - "3000"
        # expose alleen als we een db service gaan toevoegen
        ports:
            - "8181:8181"
    nginx:
        image: nginx:alpine
        ports:
            - 80:80
        volumes:
            - ./nginx.conf:/etc/nginx/nginx.conf:ro
    certbot:
        image: certbot/certbot
        volumes:
        - ./certs:/etc/letsencrypt
        - ./certbot:/var/www/certbot
        command: certbot renew --webroot --webroot-path=/var/www/certbot --email ${EMAIL} --agree-tos --no-eff-email -d ${EMAIL} --quiet --no-random-sleep-on-renew
        entrypoint: /bin/sh -c "trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;"